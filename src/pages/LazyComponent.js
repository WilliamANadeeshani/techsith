import React from 'react';

const LazyComponent = () => {
    return (
        <React.Fragment>
            <h4>Lazy Loading Component</h4>
        </React.Fragment>
    );
};

export default LazyComponent;